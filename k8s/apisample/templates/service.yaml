apiVersion: v1
kind: Service
metadata:
  name: {{ template "apisample.fullname" . }}
  labels:
    app: {{ template "apisample.name" . }}
    chart: {{ template "apisample.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: {{ template "apisample.name" . }}
    release: {{ .Release.Name }}
