apiVersion: v1
kind: Service
metadata:
  name: {{ template "betwebapi.fullname" . }}
  labels:
    app.kubernetes.io/name: {{ template "betwebapi.name" . }}
    helm.sh/chart: {{ template "betwebapi.chart" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
    app.kubernetes.io/managed-by: {{ .Release.Service }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app.kubernetes.io/name: {{ template "betwebapi.name" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
