apiVersion: v1
kind: Service
metadata:
  name: {{ template "bet-web-sample.fullname" . }}
  labels:
    app: {{ template "bet-web-sample.name" . }}
    chart: {{ template "bet-web-sample.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: {{ template "bet-web-sample.name" . }}
    release: {{ .Release.Name }}
